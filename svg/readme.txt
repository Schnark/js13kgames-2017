* simple.css is derived from ../svg.css, its contents are embeded into the SVG files.
* After changing an SVG file, embed it without the style tag into ../svg.js.
* The styles don't really need to match the original, they just have to be sensible for editing.